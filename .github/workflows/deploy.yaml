name: Deploy ON Push

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-lastest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: HealthCheck
        run: echo "Health Check Passed"